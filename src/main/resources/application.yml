# 应用服务 WEB 访问端口
server:
  port: 8080
# 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/peripheral?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456








